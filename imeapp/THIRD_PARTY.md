# Third-party dictionary notice

Cangjie single-character data used by the build comes from `rime/rime-cangjie` (`cangjie5.base.dict.yaml`).
The upstream dictionary file identifies its license as GPL. This v0.1 build uses that data only to provide local Cangjie candidate lookup.
