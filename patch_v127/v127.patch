--- project/app/src/main/assets/index.html
+++ project/app/src/main/assets/index.html
@@ -14,14 +14,14 @@
 .app-header .title{text-align:center;font-weight:850;font-size:18px;white-space:nowrap}.header-btn{background:transparent;color:#fff;padding:11px 7px;border-radius:10px;font-weight:800}.header-btn:active{background:rgba(255,255,255,.14)}
 .primary{background:var(--blue2);color:#fff;border-radius:13px;padding:11px 15px;font-weight:850}.secondary{background:#e8f2ff;color:var(--blue);border:1px solid #bcd5f2;border-radius:12px;padding:10px 13px;font-weight:800}.ghost{background:#fff;color:var(--blue);border:1px solid var(--line);border-radius:12px;padding:10px 13px;font-weight:800}.danger{background:#fff0f0;color:#a43b3b;border:1px solid #efcaca;border-radius:12px;padding:10px 13px;font-weight:850}
 #editorPage{overflow:hidden}.editor-main{flex:1;min-height:0;display:flex;flex-direction:column;background:#e8f0f8}.stage-zone{flex:1;min-height:0;position:relative;overflow:auto;background:#e8f0f8;overscroll-behavior:contain;scrollbar-width:none;touch-action:none}.stage-zone::-webkit-scrollbar{display:none}
-#stageShell{position:relative;min-width:100%;min-height:100%}#stage{position:absolute;background:#eaf2fb;box-shadow:0 4px 14px rgba(28,57,87,.14);overflow:hidden;touch-action:none;user-select:none;max-width:none;max-height:none}.piece{position:absolute;inset:0;overflow:hidden;background:#eaf2fb;touch-action:none}.piece img{position:absolute;max-width:none;max-height:none;pointer-events:none;user-select:none;-webkit-user-drag:none;transform:translate(-50%,-50%);transform-origin:center center}.piece.selected::after{content:"";position:absolute;inset:0;border:1.5px solid rgba(20,105,178,.78);pointer-events:none;clip-path:inherit;z-index:7}.photo-index{position:absolute;top:7px;left:7px;min-width:27px;height:27px;padding:0 8px;border-radius:14px;background:rgba(7,35,65,.65);color:white;display:grid;place-items:center;font-size:13px;font-weight:850;pointer-events:none;z-index:6}.date-badge{position:absolute;background:#eaf2fb;color:#17324f;font-size:22px;line-height:1;font-weight:900;white-space:nowrap;pointer-events:none;z-index:8;display:flex;align-items:center;box-sizing:border-box;overflow:hidden}
+#stageShell{position:relative;min-width:100%;min-height:100%}#stage{position:absolute;background:#eaf2fb;box-shadow:0 4px 14px rgba(28,57,87,.14);overflow:hidden;touch-action:none;user-select:none;max-width:none;max-height:none}.piece{position:absolute;inset:0;overflow:hidden;background:#eaf2fb;touch-action:none}.piece img{position:absolute;max-width:none;max-height:none;pointer-events:none;user-select:none;-webkit-user-drag:none;transform:translate(-50%,-50%);transform-origin:center center}.piece.selected::after{content:"";position:absolute;inset:0;border:1.5px solid rgba(20,105,178,.78);pointer-events:none;clip-path:inherit;z-index:7}.photo-index{position:absolute;top:7px;left:7px;min-width:27px;height:27px;padding:0 8px;border-radius:14px;background:rgba(7,35,65,.65);color:white;display:grid;place-items:center;font-size:13px;font-weight:850;pointer-events:none;z-index:6}.date-badge{position:absolute;background:#eaf2fb;color:#17324f;font-weight:900;white-space:nowrap;pointer-events:none;z-index:8;display:flex;align-items:center;box-sizing:border-box;overflow:hidden}.piece.reorder-dragging{opacity:.56}.piece.reorder-target::before{content:"";position:absolute;inset:4px;border:4px solid #2a83c7;border-radius:8px;pointer-events:none;z-index:12;box-shadow:0 0 0 2px rgba(255,255,255,.9)}
 .frame-layer{position:absolute;inset:0;width:100%;height:100%;pointer-events:none;z-index:9;overflow:visible}.frame-line{stroke:#fff;fill:none;stroke-linecap:butt;stroke-linejoin:miter}
 .boundary-layer{position:absolute;inset:0;pointer-events:none;z-index:13;overflow:visible}.boundary-svg{position:absolute;inset:0;width:100%;height:100%;overflow:visible;pointer-events:none}.boundary-line{stroke:#1d73b5;stroke-width:1.8;opacity:.96;pointer-events:none}.handle{position:absolute;width:44px;height:44px;margin:-22px 0 0 -22px;background:transparent;pointer-events:auto;touch-action:none;z-index:15}.handle::after{content:"";position:absolute;width:11px;height:11px;left:16.5px;top:16.5px;border-radius:50%;background:#fff;border:2px solid #1e69ac;box-shadow:0 1px 3px rgba(0,0,0,.2)}.handle.mid::after{width:13px;height:13px;left:15.5px;top:15.5px;background:#1e69ac;border-color:#fff}
 .mode-float{position:absolute;right:10px;top:10px;z-index:22;display:flex;background:rgba(255,255,255,.95);border-radius:13px;padding:3px;box-shadow:0 2px 9px rgba(0,0,0,.14)}.mode-float button{padding:10px 15px;border-radius:10px;background:transparent;color:#46637f;font-size:15px;font-weight:850}.mode-float button.active{background:var(--blue);color:#fff}.fit-chip{position:absolute;left:10px;top:10px;z-index:22;background:rgba(255,255,255,.95);color:var(--blue);border:1px solid #c7d9eb;border-radius:12px;padding:9px 12px;font-size:14px;font-weight:850;box-shadow:0 2px 9px rgba(0,0,0,.12)}
 .empty-stage{position:absolute;inset:0;display:flex;align-items:center;justify-content:center;z-index:5;pointer-events:none}.empty-card{pointer-events:auto;text-align:center;background:rgba(255,255,255,.94);border:1px solid #c9d9eb;border-radius:18px;padding:24px 26px;box-shadow:0 5px 18px rgba(28,57,87,.12)}.empty-card strong{display:block;font-size:20px;margin-bottom:8px}.empty-card span{display:block;color:var(--muted);font-size:14px;margin-bottom:15px}
 .gesture-tip{position:absolute;left:50%;bottom:10px;transform:translateX(-50%);z-index:22;background:rgba(11,47,82,.76);color:#fff;border-radius:12px;padding:6px 10px;font-size:12px;font-weight:750;white-space:nowrap;pointer-events:none;opacity:0;transition:opacity .25s}.gesture-tip.show{opacity:1}
 .bottom-sheet{background:#fff;border-top:1px solid var(--line);box-shadow:0 -3px 12px rgba(24,55,88,.08);z-index:25;flex:0 0 auto}.tool-tabs{height:58px;display:grid;grid-template-columns:repeat(5,1fr);border-bottom:1px solid #e4edf6}.tool-tabs button{background:#fff;color:#59718a;font-size:15px;font-weight:850;border-bottom:4px solid transparent;min-width:0}.tool-tabs button.active{color:var(--blue);border-bottom-color:var(--blue2)}.panel{display:none;min-height:112px;max-height:190px;padding:11px 12px 13px;overflow:auto}.panel.active{display:block}.layout-strip{display:flex;gap:9px;overflow-x:auto;padding:2px 1px 5px;scrollbar-width:none}.layout-strip::-webkit-scrollbar{display:none}.layout-option{flex:0 0 96px;height:86px;border:2px solid #d5e2ef;border-radius:14px;background:#f8fbff;padding:6px;display:flex;flex-direction:column;align-items:center;justify-content:center;gap:4px;color:#58718a}.layout-option.active{border-color:var(--blue2);background:#eaf4ff;color:var(--blue)}.layout-option svg{width:68px;height:44px;display:block}.layout-option span{font-size:13px;font-weight:850;white-space:nowrap}.order-row{display:flex;gap:9px;align-items:center;overflow-x:auto;padding:1px 1px 5px;min-height:92px}.order-thumb{position:relative;flex:0 0 78px;height:88px;border:2px solid transparent;border-radius:11px;overflow:hidden;background:#e8eff6;touch-action:none}.order-thumb.active{border-color:var(--blue2)}.order-thumb.dragging{opacity:.72;transform:scale(.96)}.order-thumb.drop-target{outline:3px solid #2a83c7;outline-offset:2px}.order-thumb img{width:100%;height:100%;object-fit:cover;pointer-events:none}.order-thumb b{position:absolute;left:4px;top:4px;width:22px;height:22px;border-radius:12px;background:rgba(0,0,0,.62);color:#fff;display:grid;place-items:center;font-size:12px}.date-grid{display:grid;grid-template-columns:66px minmax(145px,1fr);gap:7px 9px;align-items:center}.date-grid label{font-size:14px;font-weight:850;color:#5c7187}.date-grid input{border:1px solid #bdd0e4;border-radius:11px;padding:9px 10px;font-size:15px;color:var(--text);background:#fff;min-width:0}.toggle-row,.ratio-row,.reset-row,.adjust-row,.align-row,.frame-row,.photo-actions{display:flex;align-items:center;gap:7px;flex-wrap:wrap}.toggle-row,.align-row{margin-bottom:8px}.small-label{font-size:14px;color:#607990;font-weight:800}.pill{padding:9px 12px;font-size:14px;border:1px solid #c6d8ea;border-radius:12px;background:#f7fbff;color:#526e8a;font-weight:850}.pill.active{background:var(--blue2);color:#fff;border-color:var(--blue2)}.help-text{font-size:12px;color:#738aa0;margin-top:7px;line-height:1.45}.selected-info{font-size:14px;font-weight:850;color:#294d70;margin-bottom:8px}.photo-actions button{padding:10px 12px;font-size:14px}
-.export-body{flex:1;min-height:0;overflow:auto;padding:12px;background:#edf4fb}.preview-card{background:#fff;border-radius:16px;padding:10px;box-shadow:0 3px 12px rgba(28,57,87,.12)}#exportPreview{width:100%;height:auto;display:block;background:#eaf2fb}.form-card{margin-top:12px;background:#fff;border-radius:16px;padding:14px;box-shadow:0 3px 12px rgba(28,57,87,.1)}.form-card label{display:block;font-weight:850;margin-bottom:7px}.form-card textarea{width:100%;min-height:82px;resize:vertical;border:1px solid #bfd1e4;border-radius:12px;padding:11px;color:var(--text)}.export-actions{display:flex;gap:9px;margin-top:11px}.export-actions button{flex:1}
+.export-modal{position:fixed;inset:0;z-index:80;background:rgba(8,30,52,.46);display:flex;align-items:flex-end;justify-content:center;padding:16px}.export-modal.hidden{display:none}.export-sheet{width:min(100%,520px);background:#fff;border-radius:20px;padding:18px;box-shadow:0 14px 40px rgba(0,0,0,.22)}.export-title{font-size:20px;font-weight:900;color:var(--text);text-align:center}.export-spec{text-align:center;color:var(--muted);font-size:14px;margin:7px 0 14px}.export-actions{display:flex;gap:9px}.export-actions button{flex:1}.export-cancel{width:100%;margin-top:9px}
 @media(min-width:700px){.app-header{height:58px;min-height:58px}.stage-zone{min-height:300px}.bottom-sheet{max-height:310px}.panel{max-height:220px}}
 </style>
 </head>
@@ -29,26 +29,25 @@
 <input id="replaceInput" type="file" accept="image/*" class="hidden" />
 <section id="editorPage" class="page active">
-  <header class="app-header"><button id="addPhotoTop" class="header-btn">＋照片</button><div class="title">編輯拼圖</div><button id="toExport" class="header-btn">完成</button></header>
+  <header class="app-header"><button id="addPhotoTop" class="header-btn">＋照片</button><div class="title">編輯拼圖</div><button id="toExport" class="header-btn">輸出</button></header>
   <main class="editor-main">
     <div id="stageZone" class="stage-zone">
       <div id="stageShell"><div id="stage"></div></div>
       <button id="fitCanvas" class="fit-chip hidden">全覽</button>
       <div class="mode-float"><button id="photoModeBtn" class="active">照片</button><button id="boundaryModeBtn">邊界</button></div>
       <div id="emptyStage" class="empty-stage"><div class="empty-card"><strong>加入照片開始拼圖</strong><span>可以先選 1 張，之後再慢慢加入其他照片</span><button id="emptyAddPhoto" class="primary">＋ 加入照片</button></div></div>
-      <div id="gestureTip" class="gesture-tip">照片上兩指＝縮放照片；空白處兩指＝縮放整個拼圖</div>
+      <div id="gestureTip" class="gesture-tip">照片拖移／兩指縮放；空白處兩指縮放畫布；長按照片拖曳＝交換順序</div>
     </div>
     <div class="bottom-sheet">
-      <div class="tool-tabs"><button data-panel="layouts" class="active">版型</button><button data-panel="order">順序</button><button data-panel="dates">日期</button><button data-panel="ratio">比例</button><button data-panel="photos">照片</button></div>
+      <div class="tool-tabs"><button data-panel="layouts" class="active">版型</button><button data-panel="dates">日期</button><button data-panel="ratio">比例</button><button data-panel="frame">框線</button><button data-panel="photos">照片</button></div>
       <div id="panel-layouts" class="panel active"><div id="layoutStrip" class="layout-strip"></div></div>
-      <div id="panel-order" class="panel"><div id="orderRow" class="order-row"></div><div class="help-text">長按一張縮圖拖到另一張上，放開後兩張直接交換位置。</div></div>
       <div id="panel-dates" class="panel"><div class="toggle-row"><button id="dateToggle" class="pill active">顯示日期</button><span class="small-label">預設使用照片拍攝日</span></div><div class="align-row"><span class="small-label">位置</span><button class="pill" data-date-align="left">靠左</button><button class="pill active" data-date-align="center">置中</button><button class="pill" data-date-align="right">靠右</button></div><div id="dateGrid" class="date-grid"></div></div>
-      <div id="panel-ratio" class="panel"><div class="ratio-row"><button class="pill" data-ratio="square">1:1</button><button class="pill" data-ratio="landscape43">4:3</button><button class="pill" data-ratio="portrait34">3:4</button><button class="pill active" data-ratio="landscape169">16:9</button><button class="pill" data-ratio="portrait916">9:16</button></div><div class="frame-row" style="margin-top:9px"><span class="small-label">框線</span><button class="pill" data-frame="0">無</button><button class="pill active" data-frame="3">細</button><button class="pill" data-frame="7">中</button><button class="pill" data-frame="12">粗</button></div><div class="help-text">編輯、預覽與輸出使用相同框線；外框與內框等粗，四邊不裁切。</div></div>
-      <div id="panel-photos" class="panel"><div id="selectedInfo" class="selected-info">目前沒有照片</div><div class="photo-actions"><button id="addPhotoPanel" class="primary">＋加入照片</button><button id="replacePhoto" class="secondary">更換這張</button><button id="resetPhoto" class="secondary">重設這張</button><button id="deletePhoto" class="danger">刪除這張</button><button id="resetBoundaries" class="ghost">重設邊界</button></div><div class="help-text">更換只影響目前選取的照片；其他照片、日期與版型保留。</div></div>
+      <div id="panel-ratio" class="panel"><div class="ratio-row"><button class="pill" data-ratio="square">1:1</button><button class="pill" data-ratio="landscape43">4:3</button><button class="pill" data-ratio="portrait34">3:4</button><button class="pill active" data-ratio="landscape169">16:9</button><button class="pill" data-ratio="portrait916">9:16</button></div><div class="help-text">上方編輯區就是最後輸出比例，所見即所得。</div></div><div id="panel-frame" class="panel"><div class="frame-row"><span class="small-label">框線</span><button class="pill" data-frame="0">無</button><button class="pill active" data-frame="3">細</button><button class="pill" data-frame="7">中</button><button class="pill" data-frame="12">粗</button></div><div class="help-text">外框與內框等粗；日期區會自動為框線預留空間。</div></div>
+      <div id="panel-photos" class="panel"><div id="selectedInfo" class="selected-info">目前沒有照片</div><div class="photo-actions"><button id="addPhotoPanel" class="primary">＋加入照片</button><button id="replacePhoto" class="secondary">更換這張</button><button id="resetPhoto" class="secondary">重設這張</button><button id="deletePhoto" class="danger">刪除這張</button><button id="resetBoundaries" class="ghost">重設邊界</button></div><div class="help-text">更換只影響目前選取的照片；長按上方照片拖到另一張上，可直接交換順序。</div></div>
     </div>
   </main>
 </section>
-<section id="exportPage" class="page"><header class="app-header"><button id="backEditor" class="header-btn">← 編輯</button><div class="title">預覽與輸出</div><span></span></header><div class="export-body"><div class="preview-card"><canvas id="exportPreview"></canvas></div><div class="form-card"><label for="note">備註（可留空）</label><textarea id="note" placeholder="例如：2026/06 → 2026/09 生長比較"></textarea><div class="export-actions"><button id="savePng" class="primary">儲存 PNG</button><button id="saveJpg" class="secondary">儲存 JPG</button></div><div id="exportHelp" class="help-text">PNG 為無損輸出；JPG 使用高畫質。</div></div></div></section>
+<div id="exportModal" class="export-modal hidden"><div class="export-sheet"><div class="export-title">輸出圖片</div><div id="exportSpec" class="export-spec"></div><div class="export-actions"><button id="savePng" class="primary">儲存 PNG</button><button id="saveJpg" class="secondary">儲存 JPG</button></div><button id="cancelExport" class="ghost export-cancel">取消</button></div></div>
 <!-- compatibility-only hidden elements retained so older helper code cannot break the page -->
 <div class="hidden"><button id="backHome"></button><button id="focusOpen"></button><div id="canvasFocus"><button id="focusClose"></button><button id="focusFit"></button><button id="zoomOut"></button><button id="zoomIn"></button><button id="canvasModeBtn"></button><button id="focusPhotoModeBtn"></button><button id="focusBoundaryModeBtn"></button><button id="focusPhotoSmaller"></button><button id="focusPhotoLarger"></button><button id="focusReplace"></button><span id="zoomLabel"></span><strong id="focusTitle"></strong><div id="canvasViewport"></div><div id="canvasHolder"></div></div><button id="photoSmaller"></button><button id="photoLarger"></button><span id="photoScaleInfo"></span></div>
 <script>
@@ -72,7 +71,10 @@
 $('#addPhotoTop').onclick=chooseAdd;$('#addPhotoPanel').onclick=chooseAdd;$('#emptyAddPhoto').onclick=chooseAdd;
 $('#backHome').onclick=chooseAdd;
 $('#deletePhoto').onclick=()=>{if(!state.photos.length)return;state.photos.splice(state.selected,1);state.selected=clamp(state.selected,0,Math.max(0,state.photos.length-1));normalizeLayout();resetBoundaries();renderAllPanels();fitStage();renderEditor();};
-$('#toExport').onclick=async()=>{if(!state.photos.length){alert('請先加入照片。');return;}showPage('exportPage');await renderExportPreview();};$('#backEditor').onclick=()=>showPage('editorPage');
+function updateExportSpec(){const spec=ratioSpecs[state.ratio];$('#exportSpec').textContent=`${spec.label}｜${spec.w}×${spec.h}｜PNG 無損 / JPG 高畫質`;}
+function openExport(){if(!state.photos.length){alert('請先加入照片。');return;}updateExportSpec();$('#exportModal').classList.remove('hidden');}
+function closeExport(){$('#exportModal').classList.add('hidden');}
+$('#toExport').onclick=openExport;$('#cancelExport').onclick=closeExport;$('#exportModal').addEventListener('click',e=>{if(e.target===$('#exportModal'))closeExport();});
 function isFocus(){return false;}
 function fitBase(containerW,containerH,ar){let w=Math.max(1,containerW),h=w/ar;if(h>containerH){h=Math.max(1,containerH);w=h*ar;}return {w,h};}
 function fitStage(){const spec=ratioSpecs[state.ratio],pad=10,vw=Math.max(1,stageZone.clientWidth-pad*2),vh=Math.max(1,stageZone.clientHeight-pad*2),base=fitBase(vw,vh,spec.ar),w=Math.max(1,Math.floor(base.w*state.editorZoom)),h=Math.max(1,Math.floor(base.h*state.editorZoom)),holderW=Math.max(stageZone.clientWidth,w+pad*2),holderH=Math.max(stageZone.clientHeight,h+pad*2);stageShell.style.width=`${holderW}px`;stageShell.style.height=`${holderH}px`;stage.style.width=`${w}px`;stage.style.height=`${h}px`;stage.style.left=`${Math.floor((holderW-w)/2)}px`;stage.style.top=`${Math.floor((holderH-h)/2)}px`;$('#fitCanvas').classList.toggle('hidden',state.editorZoom<=1.01);}
@@ -100,22 +102,28 @@
 function renderEditor(){fitStage();stage.innerHTML='';if(!state.photos.length){emptyStage.classList.remove('hidden');updateAdjustPanel();return;}emptyStage.classList.add('hidden');const ps=polygons();ps.forEach((poly,i)=>{const piece=document.createElement('div');piece.className='piece'+(i===state.selected?' selected':'');piece.dataset.index=i;const img=document.createElement('img');img.src=state.photos[i].src;const idx=document.createElement('div');idx.className='photo-index';idx.textContent=String(i+1);piece.append(img,idx);if(state.showDates){const d=document.createElement('div');d.className='date-badge';d.textContent=fmtDate(state.photos[i].date);piece.appendChild(d);}attachPhotoGestures(piece,i);stage.appendChild(piece);});updatePieces();renderFrameLayer();renderBoundaryLayer();updateAdjustPanel();}
-function selectPhoto(index){state.selected=index;stage.querySelectorAll('.piece').forEach(el=>el.classList.toggle('selected',Number(el.dataset.index)===index));if($('#panel-order').classList.contains('active'))renderOrder();updateAdjustPanel();}
+function selectPhoto(index){state.selected=index;stage.querySelectorAll('.piece').forEach(el=>el.classList.toggle('selected',Number(el.dataset.index)===index));updateAdjustPanel();}
+function framePxForWidth(w){return state.frameWidth>0?Math.max(.8,state.frameWidth*(w/700)):0;}
 function dateLaneMetrics(bb,sw,sh){
-  const framePx=state.frameWidth>0?Math.max(.8,state.frameWidth*(sw/700)):0;
-  const cellH=Math.max(1,bb.h*sh);
-  const laneH=Math.min(cellH*.34,Math.max(40,34+framePx));
-  return {framePx,laneH};
+  const framePx=framePxForWidth(sw),cellW=Math.max(1,bb.w*sw),cellH=Math.max(1,bb.h*sh);
+  const baseFont=22*(sw/700),fontSize=Math.max(12,Math.min(baseFont,cellW/6.9,cellH*.18));
+  const verticalPad=Math.max(fontSize*.28,framePx*.9+4*(sw/700));
+  const laneNeed=fontSize*1.22+verticalPad*2+framePx;
+  const laneH=Math.min(cellH*.46,Math.max(fontSize*1.6+framePx*2,laneNeed));
+  const padX=Math.max(fontSize*.34,framePx*1.15+fontSize*.14);
+  return {framePx,laneH,fontSize,padX,verticalPad};
 }
 function positionDateBadge(d,bb,sw,sh){
-  const {framePx,laneH}=dateLaneMetrics(bb,sw,sh);
+  const {framePx,laneH,fontSize,padX}=dateLaneMetrics(bb,sw,sh);
   const left=bb.x*sw+framePx/2, top=(bb.y+bb.h)*sh-laneH-framePx/2;
-  const width=Math.max(1,bb.w*sw-framePx), pad=Math.max(10,framePx*.7+8);
-  d.style.left=`${left}px`; d.style.right='auto'; d.style.top=`${top}px`;
-  d.style.width=`${width}px`; d.style.height=`${laneH}px`; d.style.transform='none';
-  d.style.padding=`0 ${pad}px`;
+  const width=Math.max(1,bb.w*sw-framePx);
+  d.style.left=`${left}px`;d.style.right='auto';d.style.top=`${top}px`;
+  d.style.width=`${width}px`;d.style.height=`${laneH}px`;d.style.transform='none';
+  d.style.padding=`${Math.max(3,framePx*.55)}px ${padX}px`;
+  d.style.fontSize=`${fontSize}px`;d.style.lineHeight='1.08';
   d.style.justifyContent=state.dateAlign==='left'?'flex-start':state.dateAlign==='right'?'flex-end':'center';
 }
 function updatePieces(){
@@ -132,13 +140,24 @@
 }
 function attachPhotoGestures(el,index){el.addEventListener('wheel',e=>{if(state.mode!=='photo')return;e.preventDefault();selectPhoto(index);state.photos[index].scale=clamp(state.photos[index].scale*(e.deltaY<0?1.07:.93),.35,10);updatePieces();},{passive:false});}
-const pointers=new Map();let gesture=null;
+const pointers=new Map();let gesture=null,longPressTimer=null;
 function pDist(a,b){return Math.hypot(a.x-b.x,a.y-b.y)}function pCenter(a,b){return{x:(a.x+b.x)/2,y:(a.y+b.y)/2}}
 function pointerPieceIndex(e){const el=e.target.closest?.('.piece');return el?Number(el.dataset.index):-1;}
-function beginTwoFinger(){if(pointers.size!==2)return;const [a,b]=[...pointers.values()];const dist=Math.max(10,pDist(a,b)),c=pCenter(a,b);if(a.index>=0&&a.index===b.index&&state.mode==='photo'){selectPhoto(a.index);gesture={type:'photoPinch',index:a.index,startDist:dist,startScale:state.photos[a.index].scale};}else{gesture={type:'canvasPinch',startDist:dist,startZoom:state.editorZoom,startCenter:c,startScrollLeft:stageZone.scrollLeft,startScrollTop:stageZone.scrollTop};}}
-stageZone.addEventListener('pointerdown',e=>{if(e.target.closest?.('.handle'))return;const idx=pointerPieceIndex(e);pointers.set(e.pointerId,{x:e.clientX,y:e.clientY,index:idx,lastX:e.clientX,lastY:e.clientY});stageZone.setPointerCapture?.(e.pointerId);if(pointers.size===1&&state.mode==='photo'&&idx>=0){selectPhoto(idx);gesture={type:'photoDrag',index:idx,pointerId:e.pointerId,lastX:e.clientX,lastY:e.clientY};}else if(pointers.size===1&&idx<0&&state.editorZoom>1.01){gesture={type:'canvasPan',pointerId:e.pointerId,lastX:e.clientX,lastY:e.clientY};}else if(pointers.size===2)beginTwoFinger();e.preventDefault();},{passive:false});
-stageZone.addEventListener('pointermove',e=>{const q=pointers.get(e.pointerId);if(!q)return;q.x=e.clientX;q.y=e.clientY;pointers.set(e.pointerId,q);if(pointers.size>=2){if(!gesture||gesture.type==='photoDrag'||gesture.type==='canvasPan')beginTwoFinger();const [a,b]=[...pointers.values()].slice(0,2),dist=Math.max(10,pDist(a,b)),c=pCenter(a,b);if(gesture?.type==='photoPinch'){const p=state.photos[gesture.index];if(p){p.scale=clamp(gesture.startScale*(dist/gesture.startDist),.35,10);updatePieces();}}else if(gesture?.type==='canvasPinch'){const z=clamp(gesture.startZoom*(dist/gesture.startDist),1,4);if(Math.abs(z-state.editorZoom)>.004){const oldZoom=state.editorZoom;state.editorZoom=z;fitStage();updatePieces();renderFrameLayer();const zr=stageZone.getBoundingClientRect(),ratio=z/Math.max(.001,oldZoom),cx=c.x-zr.left,cy=c.y-zr.top;stageZone.scrollLeft=(stageZone.scrollLeft+cx)*ratio-cx;stageZone.scrollTop=(stageZone.scrollTop+cy)*ratio-cy;}stageZone.scrollLeft-=c.x-gesture.startCenter.x;stageZone.scrollTop-=c.y-gesture.startCenter.y;gesture.startCenter=c;}e.preventDefault();return;}if(gesture?.type==='photoDrag'&&gesture.pointerId===e.pointerId&&state.mode==='photo'){const p=state.photos[gesture.index],poly=polygons()[gesture.index],bb=bbox(poly);if(p){p.panX=clamp(p.panX+(e.clientX-gesture.lastX)/Math.max(60,bb.w*stage.clientWidth),-1.8,1.8);p.panY=clamp(p.panY+(e.clientY-gesture.lastY)/Math.max(60,bb.h*stage.clientHeight),-1.8,1.8);gesture.lastX=e.clientX;gesture.lastY=e.clientY;updatePieces();}e.preventDefault();return;}if(gesture?.type==='canvasPan'&&gesture.pointerId===e.pointerId){stageZone.scrollLeft-=e.clientX-gesture.lastX;stageZone.scrollTop-=e.clientY-gesture.lastY;gesture.lastX=e.clientX;gesture.lastY=e.clientY;e.preventDefault();}},{passive:false});
-function endPointer(e){pointers.delete(e.pointerId);if(!pointers.size)gesture=null;else if(pointers.size===1&&gesture?.type!=='photoDrag'&&gesture?.type!=='canvasPan')gesture=null;}stageZone.addEventListener('pointerup',endPointer);stageZone.addEventListener('pointercancel',endPointer);
+function pointInPoly(x,y,poly){let inside=false;for(let i=0,j=poly.length-1;i<poly.length;j=i++){const xi=poly[i][0],yi=poly[i][1],xj=poly[j][0],yj=poly[j][1];const hit=((yi>y)!==(yj>y))&&(x<(xj-xi)*(y-yi)/((yj-yi)||1e-9)+xi);if(hit)inside=!inside;}return inside;}
+function stageIndexAtClient(cx,cy){const r=stage.getBoundingClientRect();if(cx<r.left||cx>r.right||cy<r.top||cy>r.bottom)return-1;const x=(cx-r.left)/r.width,y=(cy-r.top)/r.height,ps=polygons();for(let i=0;i<ps.length;i++)if(pointInPoly(x,y,ps[i]))return i;return-1;}
+function clearLongPress(){if(longPressTimer){clearTimeout(longPressTimer);longPressTimer=null;}}
+function clearReorderVisual(){stage.querySelectorAll('.piece').forEach(x=>x.classList.remove('reorder-dragging','reorder-target'));}
+function updateReorderTarget(t){stage.querySelectorAll('.piece').forEach(x=>x.classList.remove('reorder-target'));if(t>=0){const el=stage.querySelector(`.piece[data-index="${t}"]`);el?.classList.add('reorder-target');}}
+function startLongPressReorder(index,pointerId){clearLongPress();longPressTimer=setTimeout(()=>{if(pointers.size!==1||gesture?.type!=='photoPending'||gesture.pointerId!==pointerId)return;gesture={type:'reorder',source:index,target:index,pointerId};stage.querySelector(`.piece[data-index="${index}"]`)?.classList.add('reorder-dragging');updateReorderTarget(index);},460);}
+function beginTwoFinger(){clearLongPress();if(pointers.size!==2)return;clearReorderVisual();const [a,b]=[...pointers.values()];const dist=Math.max(10,pDist(a,b)),c=pCenter(a,b);if(a.index>=0&&a.index===b.index&&state.mode==='photo'){selectPhoto(a.index);gesture={type:'photoPinch',index:a.index,startDist:dist,startScale:state.photos[a.index].scale};}else{gesture={type:'canvasPinch',startDist:dist,startZoom:state.editorZoom,startCenter:c,startScrollLeft:stageZone.scrollLeft,startScrollTop:stageZone.scrollTop};}}
+stageZone.addEventListener('pointerdown',e=>{if(e.target.closest?.('.handle'))return;const idx=pointerPieceIndex(e);pointers.set(e.pointerId,{x:e.clientX,y:e.clientY,index:idx,lastX:e.clientX,lastY:e.clientY});stageZone.setPointerCapture?.(e.pointerId);if(pointers.size===1&&state.mode==='photo'&&idx>=0){selectPhoto(idx);gesture={type:'photoPending',index:idx,pointerId:e.pointerId,startX:e.clientX,startY:e.clientY,lastX:e.clientX,lastY:e.clientY};startLongPressReorder(idx,e.pointerId);}else if(pointers.size===1&&idx<0&&state.editorZoom>1.01){gesture={type:'canvasPan',pointerId:e.pointerId,lastX:e.clientX,lastY:e.clientY};}else if(pointers.size===2)beginTwoFinger();e.preventDefault();},{passive:false});
+stageZone.addEventListener('pointermove',e=>{const q=pointers.get(e.pointerId);if(!q)return;q.x=e.clientX;q.y=e.clientY;pointers.set(e.pointerId,q);if(pointers.size>=2){if(!gesture||['photoPending','photoDrag','canvasPan','reorder'].includes(gesture.type))beginTwoFinger();const [a,b]=[...pointers.values()].slice(0,2),dist=Math.max(10,pDist(a,b)),c=pCenter(a,b);if(gesture?.type==='photoPinch'){const p=state.photos[gesture.index];if(p){p.scale=clamp(gesture.startScale*(dist/gesture.startDist),.35,10);updatePieces();}}else if(gesture?.type==='canvasPinch'){const z=clamp(gesture.startZoom*(dist/gesture.startDist),1,4);if(Math.abs(z-state.editorZoom)>.004){const oldZoom=state.editorZoom;state.editorZoom=z;fitStage();updatePieces();renderFrameLayer();const zr=stageZone.getBoundingClientRect(),ratio=z/Math.max(.001,oldZoom),cx=c.x-zr.left,cy=c.y-zr.top;stageZone.scrollLeft=(stageZone.scrollLeft+cx)*ratio-cx;stageZone.scrollTop=(stageZone.scrollTop+cy)*ratio-cy;}stageZone.scrollLeft-=c.x-gesture.startCenter.x;stageZone.scrollTop-=c.y-gesture.startCenter.y;gesture.startCenter=c;}e.preventDefault();return;}
+if(gesture?.type==='photoPending'&&gesture.pointerId===e.pointerId){if(Math.hypot(e.clientX-gesture.startX,e.clientY-gesture.startY)>7){clearLongPress();gesture={type:'photoDrag',index:gesture.index,pointerId:e.pointerId,lastX:gesture.startX,lastY:gesture.startY};}else{e.preventDefault();return;}}
+if(gesture?.type==='reorder'&&gesture.pointerId===e.pointerId){const t=stageIndexAtClient(e.clientX,e.clientY);gesture.target=t;updateReorderTarget(t);e.preventDefault();return;}
+if(gesture?.type==='photoDrag'&&gesture.pointerId===e.pointerId&&state.mode==='photo'){const p=state.photos[gesture.index],poly=polygons()[gesture.index],bb=bbox(poly);if(p){p.panX=clamp(p.panX+(e.clientX-gesture.lastX)/Math.max(60,bb.w*stage.clientWidth),-1.8,1.8);p.panY=clamp(p.panY+(e.clientY-gesture.lastY)/Math.max(60,bb.h*stage.clientHeight),-1.8,1.8);gesture.lastX=e.clientX;gesture.lastY=e.clientY;updatePieces();}e.preventDefault();return;}if(gesture?.type==='canvasPan'&&gesture.pointerId===e.pointerId){stageZone.scrollLeft-=e.clientX-gesture.lastX;stageZone.scrollTop-=e.clientY-gesture.lastY;gesture.lastX=e.clientX;gesture.lastY=e.clientY;e.preventDefault();}},{passive:false});
+function endPointer(e){clearLongPress();const g=gesture;if(g?.type==='reorder'&&g.pointerId===e.pointerId){const a=g.source,b=g.target;clearReorderVisual();if(b>=0&&b<state.photos.length&&a!==b){[state.photos[a],state.photos[b]]=[state.photos[b],state.photos[a]];state.selected=b;renderEditor();renderDates();updateAdjustPanel();}}pointers.delete(e.pointerId);if(!pointers.size){gesture=null;clearReorderVisual();}else if(pointers.size===1&&!['photoDrag','canvasPan'].includes(gesture?.type))gesture=null;}stageZone.addEventListener('pointerup',endPointer);stageZone.addEventListener('pointercancel',endPointer);
 $('#fitCanvas').onclick=()=>{state.editorZoom=1;stageZone.scrollLeft=0;stageZone.scrollTop=0;fitStage();updatePieces();renderFrameLayer();};
 function showGestureTip(){const t=$('#gestureTip');t.classList.add('show');clearTimeout(window._gestureTip);window._gestureTip=setTimeout(()=>t.classList.remove('show'),3200);}
 function updateModeButtons(){const p=state.mode==='photo',b=state.mode==='boundary';$('#photoModeBtn').classList.toggle('active',p);$('#boundaryModeBtn').classList.toggle('active',b);}
@@ -148,20 +167,17 @@
 function renderBoundaryLayer(){if(state.mode!=='boundary'||!state.photos.length)return;ensureActiveBoundary();const b=state.boundaries,active=boundarySegments().find(s=>s.key===state.activeBoundary);if(!active)return;const layer=document.createElement('div');layer.className='boundary-layer';const svg=document.createElementNS('http://www.w3.org/2000/svg','svg');svg.setAttribute('class','boundary-svg');layer.appendChild(svg);const l=document.createElementNS('http://www.w3.org/2000/svg','line');l.setAttribute('class','boundary-line');svg.appendChild(l);stage.appendChild(layer);let h1=null,h2=null,hm=null;const refresh=()=>{const a=boundarySegments().find(s=>s.key===state.activeBoundary);if(!a)return;l.setAttribute('x1',a.p1[0]*100+'%');l.setAttribute('y1',a.p1[1]*100+'%');l.setAttribute('x2',a.p2[0]*100+'%');l.setAttribute('y2',a.p2[1]*100+'%');if(hm)posHandle(hm,(a.p1[0]+a.p2[0])/2,(a.p1[1]+a.p2[1])/2);if(h1){if(a.kind==='row'||a.kind==='leftMain')posHandle(h1,a.p1[0],.02);else posHandle(h1,.02,a.p1[1]);}if(h2){if(a.kind==='row'||a.kind==='leftMain')posHandle(h2,a.p2[0],.98);else posHandle(h2,.98,a.p2[1]);}updatePieces();renderFrameLayer();};const k=active.kind;if(['row','col','leftMain','topMain'].includes(k)){h1=createHandle(layer);h2=createHandle(layer);hm=createHandle(layer,'mid');}else hm=createHandle(layer,'mid');if(k==='row'){const i=active.index,ln=b.lines[i];dragHandle(h1,x=>{ln.a=x;constrainRowLine(i);refresh();});dragHandle(h2,x=>{ln.b=x;constrainRowLine(i);refresh();});dragHandle(hm,x=>{const m=(ln.a+ln.b)/2,d=x-m;ln.a+=d;ln.b+=d;constrainRowLine(i);refresh();});}else if(k==='col'){const i=active.index,ln=b.lines[i];dragHandle(h1,(x,y)=>{ln.a=y;constrainColLine(i);refresh();});dragHandle(h2,(x,y)=>{ln.b=y;constrainColLine(i);refresh();});dragHandle(hm,(x,y)=>{const m=(ln.a+ln.b)/2,d=y-m;ln.a+=d;ln.b+=d;constrainColLine(i);refresh();});}else if(k==='gridX')dragHandle(hm,x=>{b.x=clamp(x,.18,.82);refresh();});else if(k==='gridY')dragHandle(hm,(x,y)=>{b.y=clamp(y,.18,.82);refresh();});else if(k==='leftMain'){const v=b.v;dragHandle(h1,x=>{v.a=clamp(x,.18,.84);refresh();});dragHandle(h2,x=>{v.b=clamp(x,.18,.84);refresh();});dragHandle(hm,x=>{const m=(v.a+v.b)/2,d=x-m;v.a=clamp(v.a+d,.18,.84);v.b=clamp(v.b+d,.18,.84);refresh();});}else if(k==='leftSub'){const i=active.index;dragHandle(hm,(x,y)=>{const lo=i===0?.12:b.ys[i-1]+.10,hi=i===b.ys.length-1?.88:b.ys[i+1]-.10;b.ys[i]=clamp(y,lo,hi);refresh();});}else if(k==='topMain'){const h=b.h;dragHandle(h1,(x,y)=>{h.a=clamp(y,.18,.84);refresh();});dragHandle(h2,(x,y)=>{h.b=clamp(y,.18,.84);refresh();});dragHandle(hm,(x,y)=>{const m=(h.a+h.b)/2,d=y-m;h.a=clamp(h.a+d,.18,.84);h.b=clamp(h.b+d,.18,.84);refresh();});}else if(k==='topSub'){const i=active.index;dragHandle(hm,(x,y)=>{const lo=i===0?.12:b.xs[i-1]+.10,hi=i===b.xs.length-1?.88:b.xs[i+1]-.10;b.xs[i]=clamp(x,lo,hi);refresh();});}refresh();}
-function renderFrameLayer(){stage.querySelector('.frame-layer')?.remove();if(!state.photos.length||state.frameWidth<=0)return;const sw=stage.clientWidth,sh=stage.clientHeight;if(!sw||!sh)return;const fw=Math.max(.8,state.frameWidth*(sw/700)),svg=document.createElementNS('http://www.w3.org/2000/svg','svg');svg.setAttribute('class','frame-layer');svg.setAttribute('viewBox',`0 0 ${sw} ${sh}`);const rect=document.createElementNS('http://www.w3.org/2000/svg','rect');rect.setAttribute('class','frame-line');rect.setAttribute('x',fw/2);rect.setAttribute('y',fw/2);rect.setAttribute('width',Math.max(0,sw-fw));rect.setAttribute('height',Math.max(0,sh-fw));rect.setAttribute('stroke-width',fw);svg.appendChild(rect);for(const seg of boundarySegments()){const ln=document.createElementNS('http://www.w3.org/2000/svg','line');ln.setAttribute('class','frame-line');ln.setAttribute('x1',seg.p1[0]*sw);ln.setAttribute('y1',seg.p1[1]*sh);ln.setAttribute('x2',seg.p2[0]*sw);ln.setAttribute('y2',seg.p2[1]*sh);ln.setAttribute('stroke-width',fw);svg.appendChild(ln);}stage.appendChild(svg);}
-function renderOrder(){const row=$('#orderRow');row.innerHTML='';state.photos.forEach((p,i)=>{const b=document.createElement('button');b.className='order-thumb'+(i===state.selected?' active':'');b.dataset.index=i;b.innerHTML=`<img src="${p.src}"><b>${i+1}</b>`;let timer=null,dragging=false,target=i;b.addEventListener('pointerdown',e=>{target=i;timer=setTimeout(()=>{dragging=true;b.classList.add('dragging');b.setPointerCapture?.(e.pointerId);},260);});b.addEventListener('pointermove',e=>{if(!dragging)return;const hit=document.elementFromPoint(e.clientX,e.clientY)?.closest?.('.order-thumb');row.querySelectorAll('.order-thumb').forEach(x=>x.classList.remove('drop-target'));if(hit){target=Number(hit.dataset.index);hit.classList.add('drop-target');}});const finish=e=>{clearTimeout(timer);row.querySelectorAll('.order-thumb').forEach(x=>x.classList.remove('drop-target'));b.classList.remove('dragging');if(dragging&&target!==i&&target>=0&&target<state.photos.length){[state.photos[i],state.photos[target]]=[state.photos[target],state.photos[i]];state.selected=target;renderEditor();renderOrder();renderDates();}else if(!dragging){state.selected=i;renderEditor();renderOrder();updateAdjustPanel();}dragging=false;};b.addEventListener('pointerup',finish);b.addEventListener('pointercancel',finish);row.appendChild(b);});}
+function renderFrameLayer(){stage.querySelector('.frame-layer')?.remove();if(!state.photos.length||state.frameWidth<=0)return;const sw=stage.clientWidth,sh=stage.clientHeight;if(!sw||!sh)return;const fw=framePxForWidth(sw),svg=document.createElementNS('http://www.w3.org/2000/svg','svg');svg.setAttribute('class','frame-layer');svg.setAttribute('viewBox',`0 0 ${sw} ${sh}`);const rect=document.createElementNS('http://www.w3.org/2000/svg','rect');rect.setAttribute('class','frame-line');rect.setAttribute('x',fw/2);rect.setAttribute('y',fw/2);rect.setAttribute('width',Math.max(0,sw-fw));rect.setAttribute('height',Math.max(0,sh-fw));rect.setAttribute('stroke-width',fw);svg.appendChild(rect);for(const seg of boundarySegments()){const ln=document.createElementNS('http://www.w3.org/2000/svg','line');ln.setAttribute('class','frame-line');ln.setAttribute('x1',seg.p1[0]*sw);ln.setAttribute('y1',seg.p1[1]*sh);ln.setAttribute('x2',seg.p2[0]*sw);ln.setAttribute('y2',seg.p2[1]*sh);ln.setAttribute('stroke-width',fw);svg.appendChild(ln);}stage.appendChild(svg);}
 function renderDates(){const g=$('#dateGrid');g.innerHTML='';state.photos.forEach((p,i)=>{const l=document.createElement('label');l.textContent=`第 ${i+1} 張`;const input=document.createElement('input');input.type='date';input.value=p.date;input.onchange=()=>{p.date=input.value;renderEditor();};g.append(l,input);});$('#dateToggle').classList.toggle('active',state.showDates);$$('[data-date-align]').forEach(b=>b.classList.toggle('active',b.dataset.dateAlign===state.dateAlign));}
 $('#dateToggle').onclick=()=>{state.showDates=!state.showDates;renderDates();renderEditor();};$$('[data-date-align]').forEach(b=>b.onclick=()=>{state.dateAlign=b.dataset.dateAlign;renderDates();renderEditor();});
-function renderAllPanels(){renderLayoutStrip();renderOrder();renderDates();$$('[data-ratio]').forEach(b=>b.classList.toggle('active',b.dataset.ratio===state.ratio));$$('[data-frame]').forEach(b=>b.classList.toggle('active',Number(b.dataset.frame)===state.frameWidth));updateAdjustPanel();}
-$$('.tool-tabs button').forEach(b=>b.onclick=()=>{$$('.tool-tabs button').forEach(x=>x.classList.toggle('active',x===b));$$('.panel').forEach(p=>p.classList.toggle('active',p.id===`panel-${b.dataset.panel}`));if(b.dataset.panel==='order')renderOrder();if(b.dataset.panel==='dates')renderDates();if(b.dataset.panel==='photos')updateAdjustPanel();});
-$$('[data-ratio]').forEach(b=>b.onclick=()=>{state.ratio=b.dataset.ratio;$$('[data-ratio]').forEach(x=>x.classList.toggle('active',x===b));requestAnimationFrame(()=>{fitStage();renderEditor();});});
-$$('[data-frame]').forEach(b=>b.onclick=()=>{state.frameWidth=Number(b.dataset.frame);$$('[data-frame]').forEach(x=>x.classList.toggle('active',x===b));renderEditor();});
+function renderAllPanels(){renderLayoutStrip();renderDates();$$('[data-ratio]').forEach(b=>b.classList.toggle('active',b.dataset.ratio===state.ratio));$$('[data-frame]').forEach(b=>b.classList.toggle('active',Number(b.dataset.frame)===state.frameWidth));updateAdjustPanel();}
+$$('.tool-tabs button').forEach(b=>b.onclick=()=>{$$('.tool-tabs button').forEach(x=>x.classList.toggle('active',x===b));$$('.panel').forEach(p=>p.classList.toggle('active',p.id===`panel-${b.dataset.panel}`));if(b.dataset.panel==='dates')renderDates();if(b.dataset.panel==='photos')updateAdjustPanel();});
+$$('[data-ratio]').forEach(b=>b.onclick=()=>{state.ratio=b.dataset.ratio;$$('[data-ratio]').forEach(x=>x.classList.toggle('active',x===b));requestAnimationFrame(()=>{fitStage();renderEditor();updateExportSpec();});});
+$$('[data-frame]').forEach(b=>b.onclick=()=>{state.frameWidth=Number(b.dataset.frame);$$('[data-frame]').forEach(x=>x.classList.toggle('active',x===b));renderEditor();updateExportSpec();});
 function changePhotoScale(mult){const p=state.photos[state.selected];if(!p)return;p.scale=clamp(p.scale*mult,.45,8);updatePieces();updateAdjustPanel();}
 function updateAdjustPanel(){const p=state.photos[state.selected];$('#selectedInfo').textContent=p?`目前：第 ${state.selected+1} 張`:'目前沒有照片';$('#replacePhoto').disabled=!p;$('#resetPhoto').disabled=!p;$('#deletePhoto').disabled=!p;}
 $('#photoSmaller').onclick=()=>changePhotoScale(.9);$('#photoLarger').onclick=()=>changePhotoScale(1.1);$('#focusPhotoSmaller').onclick=()=>changePhotoScale(.9);$('#focusPhotoLarger').onclick=()=>changePhotoScale(1.1);$('#resetPhoto').onclick=()=>{const p=state.photos[state.selected];if(!p)return;p.panX=0;p.panY=0;p.scale=1;renderEditor();};$('#resetBoundaries').onclick=()=>{resetBoundaries();renderEditor();};
@@ -171,58 +187,32 @@
 function clipPoly(ctx,poly,W,H){ctx.beginPath();poly.forEach(([x,y],i)=>{const X=x*W,Y=y*H;i?ctx.lineTo(X,Y):ctx.moveTo(X,Y)});ctx.closePath();ctx.clip();}
 async function loadImg(src){return await new Promise((res,rej)=>{const i=new Image();i.onload=()=>res(i);i.onerror=rej;i.src=src;});}
 function dateDrawX(bb,W,tw,pad){if(state.dateAlign==='left')return {x:bb.x*W+pad,align:'left',rx:bb.x*W+pad*.5};if(state.dateAlign==='right')return {x:(bb.x+bb.w)*W-pad,align:'right',rx:(bb.x+bb.w)*W-tw-pad*1.5};return {x:(bb.x+bb.w/2)*W,align:'center',rx:(bb.x+bb.w/2)*W-tw/2-pad};}
 async function buildCanvas(){
-  const spec=ratioSpecs[state.ratio],W=spec.w,H=spec.h,note=$('#note').value.trim(),
-        noteH=note?Math.round(H*.09):0,imageH=H-noteH,
-        fw=state.frameWidth>0?Math.max(2,state.frameWidth*(W/700)):0,
-        c=document.createElement('canvas');
+  const spec=ratioSpecs[state.ratio],W=spec.w,H=spec.h,fw=framePxForWidth(W),c=document.createElement('canvas');
   c.width=W;c.height=H;
   const ctx=c.getContext('2d');
   ctx.fillStyle='#eaf2fb';ctx.fillRect(0,0,W,H);
   const ps=polygons();
   for(let i=0;i<state.photos.length;i++){
     const p=state.photos[i],poly=ps[i],bb=bbox(poly),img=await loadImg(p.src);
-    ctx.save();
-    clipPoly(ctx,poly,W,imageH);
-    ctx.fillStyle='#eaf2fb';ctx.fillRect(0,0,W,imageH);
-    const bw=bb.w*W,fullH=bb.h*imageH;
-    let laneH=0,size=0;
-    if(state.showDates){
-      size=Math.max(44,Math.round(Math.min(W,H)*.044));
-      laneH=Math.min(fullH*.34,Math.max(size*1.55,fw+size*1.28));
-    }
+    ctx.save();clipPoly(ctx,poly,W,H);ctx.fillStyle='#eaf2fb';ctx.fillRect(0,0,W,H);
+    const bw=bb.w*W,fullH=bb.h*H,m=dateLaneMetrics(bb,W,H),laneH=state.showDates?m.laneH:0;
     const photoH=Math.max(1,fullH-laneH);
     const fit=Math.min(bw/p.w,photoH/p.h),dw=p.w*fit*p.scale,dh=p.h*fit*p.scale;
     const cx=(bb.x+bb.w/2+p.panX*bb.w)*W;
-    const cy=bb.y*imageH+photoH/2+p.panY*photoH;
+    const cy=bb.y*H+photoH/2+p.panY*photoH;
     ctx.drawImage(img,cx-dw/2,cy-dh/2,dw,dh);
     if(state.showDates){
       const txt=fmtDate(p.date);
       if(txt){
-        const laneX=bb.x*W,laneY=(bb.y+bb.h)*imageH-laneH;
+        const laneX=bb.x*W,laneY=(bb.y+bb.h)*H-laneH;
         ctx.fillStyle='#eaf2fb';ctx.fillRect(laneX,laneY,bw,laneH);
-        const pad=Math.max(size*.36,fw*.75+size*.12);
-        ctx.font=`850 ${size}px system-ui,sans-serif`;
+        ctx.font=`900 ${m.fontSize}px system-ui,sans-serif`;
         ctx.fillStyle='#17324f';ctx.textBaseline='middle';
-        if(state.dateAlign==='left'){ctx.textAlign='left';ctx.fillText(txt,laneX+pad,laneY+laneH/2);}
-        else if(state.dateAlign==='right'){ctx.textAlign='right';ctx.fillText(txt,laneX+bw-pad,laneY+laneH/2);}
+        if(state.dateAlign==='left'){ctx.textAlign='left';ctx.fillText(txt,laneX+m.padX,laneY+laneH/2);}
+        else if(state.dateAlign==='right'){ctx.textAlign='right';ctx.fillText(txt,laneX+bw-m.padX,laneY+laneH/2);}
         else {ctx.textAlign='center';ctx.fillText(txt,laneX+bw/2,laneY+laneH/2);}
       }
     }
@@ -231,20 +221,11 @@
   if(state.frameWidth>0){
     ctx.save();ctx.strokeStyle='#fff';ctx.lineJoin='miter';ctx.lineCap='butt';ctx.lineWidth=fw;
-    for(const seg of boundarySegments()){
-      ctx.beginPath();ctx.moveTo(seg.p1[0]*W,seg.p1[1]*imageH);ctx.lineTo(seg.p2[0]*W,seg.p2[1]*imageH);ctx.stroke();
-    }
-    ctx.strokeRect(fw/2,fw/2,Math.max(0,W-fw),Math.max(0,imageH-fw));
-    ctx.restore();
-  }
-  if(noteH){
-    ctx.fillStyle='#fff';ctx.fillRect(0,imageH,W,noteH);
-    ctx.strokeStyle='#d4e0ed';ctx.lineWidth=2;ctx.beginPath();ctx.moveTo(0,imageH);ctx.lineTo(W,imageH);ctx.stroke();
-    ctx.fillStyle='#17324f';ctx.textAlign='center';ctx.textBaseline='middle';
-    ctx.font=`650 ${Math.round(noteH*.32)}px system-ui,sans-serif`;
-    drawWrap(ctx,note,W/2,imageH+noteH/2,W*.9,noteH*.38,2);
+    for(const seg of boundarySegments()){ctx.beginPath();ctx.moveTo(seg.p1[0]*W,seg.p1[1]*H);ctx.lineTo(seg.p2[0]*W,seg.p2[1]*H);ctx.stroke();}
+    ctx.strokeRect(fw/2,fw/2,Math.max(0,W-fw),Math.max(0,H-fw));ctx.restore();
   }
   return c;
 }
-function roundRect(ctx,x,y,w,h,r){const rr=Math.min(r,w/2,h/2);ctx.beginPath();ctx.moveTo(x+rr,y);ctx.arcTo(x+w,y,x+w,y+h,rr);ctx.arcTo(x+w,y+h,x,y+h,rr);ctx.arcTo(x,y+h,x,y,rr);ctx.arcTo(x,y,x+w,y,rr);ctx.closePath();}
-function drawWrap(ctx,text,x,y,maxW,lineH,maxLines){let lines=[''];for(const ch of [...text]){let t=lines[lines.length-1]+ch;if(ctx.measureText(t).width>maxW&&lines[lines.length-1]){if(lines.length>=maxLines)break;lines.push(ch);}else lines[lines.length-1]=t;}const total=(lines.length-1)*lineH;lines.forEach((ln,i)=>ctx.fillText(ln,x,y-total/2+i*lineH));}
-async function renderExportPreview(){const c=await buildCanvas(),p=$('#exportPreview');p.width=Math.round(c.width*.25);p.height=Math.round(c.height*.25);p.getContext('2d').drawImage(c,0,0,p.width,p.height);const spec=ratioSpecs[state.ratio];$('#exportHelp').textContent=`PNG 為無損輸出；JPG 使用高畫質。${spec.label} 輸出 ${spec.w}×${spec.h}。`;}
-$('#note').addEventListener('input',()=>{clearTimeout(window._pv);window._pv=setTimeout(renderExportPreview,180);});
-async function save(kind){const c=await buildCanvas(),mime=kind==='png'?'image/png':'image/jpeg',ext=kind==='png'?'png':'jpg',quality=kind==='png'?undefined:.98,data=c.toDataURL(mime,quality),name=`照片拼圖_${new Date().toISOString().slice(0,10)}.${ext}`;if(window.Android&&typeof Android.saveBase64File==='function')Android.saveBase64File(name,mime,data);else{const a=document.createElement('a');a.href=data;a.download=name;a.click();}}
+async function save(kind){const c=await buildCanvas(),mime=kind==='png'?'image/png':'image/jpeg',ext=kind==='png'?'png':'jpg',quality=kind==='png'?undefined:.98,data=c.toDataURL(mime,quality),name=`照片拼圖_${new Date().toISOString().slice(0,10)}.${ext}`;if(window.Android&&typeof Android.saveBase64File==='function')Android.saveBase64File(name,mime,data);else{const a=document.createElement('a');a.href=data;a.download=name;a.click();}closeExport();}
 $('#savePng').onclick=()=>save('png');$('#saveJpg').onclick=()=>save('jpg');
 updateModeButtons();showPage('editorPage');renderAllPanels();requestAnimationFrame(()=>{fitStage();renderEditor();});
 </script>
