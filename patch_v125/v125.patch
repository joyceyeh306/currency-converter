--- project/app/src/main/assets/index.html
+++ project/app/src/main/assets/index.html
@@ -13,7 +13,7 @@
 .app-header{height:52px;min-height:52px;background:var(--blue);color:#fff;display:grid;grid-template-columns:90px 1fr 90px;align-items:center;padding:0 8px;box-shadow:0 2px 8px rgba(0,0,0,.14);z-index:30}
 .app-header .title{text-align:center;font-weight:850;font-size:18px;white-space:nowrap}.header-btn{background:transparent;color:#fff;padding:11px 7px;border-radius:10px;font-weight:800}.header-btn:active{background:rgba(255,255,255,.14)}
 .primary{background:var(--blue2);color:#fff;border-radius:13px;padding:11px 15px;font-weight:850}.secondary{background:#e8f2ff;color:var(--blue);border:1px solid #bcd5f2;border-radius:12px;padding:10px 13px;font-weight:800}.ghost{background:#fff;color:var(--blue);border:1px solid var(--line);border-radius:12px;padding:10px 13px;font-weight:800}.danger{background:#fff0f0;color:#a43b3b;border:1px solid #efcaca;border-radius:12px;padding:10px 13px;font-weight:850}
-#editorPage{overflow:hidden}.editor-main{flex:1;min-height:0;display:flex;flex-direction:column;background:#e8f0f8}.stage-zone{flex:1;min-height:0;position:relative;overflow:auto;background:#e8f0f8;overscroll-behavior:contain;scrollbar-width:none}.stage-zone::-webkit-scrollbar{display:none}
+#editorPage{overflow:hidden}.editor-main{flex:1;min-height:0;display:flex;flex-direction:column;background:#e8f0f8}.stage-zone{flex:1;min-height:0;position:relative;overflow:auto;background:#e8f0f8;overscroll-behavior:contain;scrollbar-width:none;touch-action:none}.stage-zone::-webkit-scrollbar{display:none}
 #stageShell{position:relative;min-width:100%;min-height:100%}#stage{position:absolute;background:#eaf2fb;box-shadow:0 4px 14px rgba(28,57,87,.14);overflow:hidden;touch-action:none;user-select:none;max-width:none;max-height:none}.piece{position:absolute;inset:0;overflow:hidden;background:#eaf2fb;touch-action:none}.piece img{position:absolute;max-width:none;max-height:none;pointer-events:none;user-select:none;-webkit-user-drag:none;transform:translate(-50%,-50%);transform-origin:center center}.piece.selected::after{content:"";position:absolute;inset:0;border:1.5px solid rgba(20,105,178,.78);pointer-events:none;clip-path:inherit;z-index:7}.photo-index{position:absolute;top:7px;left:7px;min-width:27px;height:27px;padding:0 8px;border-radius:14px;background:rgba(7,35,65,.65);color:white;display:grid;place-items:center;font-size:13px;font-weight:850;pointer-events:none;z-index:6}.date-badge{position:absolute;padding:5px 10px;border-radius:9px;background:rgba(255,255,255,.88);color:#17324f;font-size:22px;line-height:1.15;font-weight:900;white-space:nowrap;pointer-events:none;box-shadow:0 1px 4px rgba(0,0,0,.08);z-index:8}
 .frame-layer{position:absolute;inset:0;width:100%;height:100%;pointer-events:none;z-index:9;overflow:visible}.frame-line{stroke:#fff;fill:none;stroke-linecap:butt;stroke-linejoin:miter}
 .boundary-layer{position:absolute;inset:0;pointer-events:none;z-index:13;overflow:visible}.boundary-svg{position:absolute;inset:0;width:100%;height:100%;overflow:visible;pointer-events:none}.boundary-line{stroke:#1d73b5;stroke-width:1.8;opacity:.96;pointer-events:none}.handle{position:absolute;width:44px;height:44px;margin:-22px 0 0 -22px;background:transparent;pointer-events:auto;touch-action:none;z-index:15}.handle::after{content:"";position:absolute;width:11px;height:11px;left:16.5px;top:16.5px;border-radius:50%;background:#fff;border:2px solid #1e69ac;box-shadow:0 1px 3px rgba(0,0,0,.2)}.handle.mid::after{width:13px;height:13px;left:15.5px;top:15.5px;background:#1e69ac;border-color:#fff}
@@ -36,7 +36,7 @@
       <button id="fitCanvas" class="fit-chip hidden">全覽</button>
       <div class="mode-float"><button id="photoModeBtn" class="active">照片</button><button id="boundaryModeBtn">邊界</button></div>
       <div id="emptyStage" class="empty-stage"><div class="empty-card"><strong>加入照片開始拼圖</strong><span>可以先選 1 張，之後再慢慢加入其他照片</span><button id="emptyAddPhoto" class="primary">＋ 加入照片</button></div></div>
-      <div id="gestureTip" class="gesture-tip">兩指同一張＝縮放照片；兩指跨不同照片＝縮放整個拼圖</div>
+      <div id="gestureTip" class="gesture-tip">照片上兩指＝縮放照片；空白處兩指＝縮放整個拼圖</div>
     </div>
     <div class="bottom-sheet">
       <div class="tool-tabs"><button data-panel="layouts" class="active">版型</button><button data-panel="order">順序</button><button data-panel="dates">日期</button><button data-panel="ratio">比例</button><button data-panel="photos">照片</button></div>
@@ -91,16 +91,16 @@
 function bbox(poly){const xs=poly.map(p=>p[0]),ys=poly.map(p=>p[1]);return {x:Math.min(...xs),y:Math.min(...ys),w:Math.max(...xs)-Math.min(...xs),h:Math.max(...ys)-Math.min(...ys)};}
 function renderEditor(){fitStage();stage.innerHTML='';if(!state.photos.length){emptyStage.classList.remove('hidden');updateAdjustPanel();return;}emptyStage.classList.add('hidden');const ps=polygons();ps.forEach((poly,i)=>{const piece=document.createElement('div');piece.className='piece'+(i===state.selected?' selected':'');piece.dataset.index=i;const img=document.createElement('img');img.src=state.photos[i].src;const idx=document.createElement('div');idx.className='photo-index';idx.textContent=String(i+1);piece.append(img,idx);if(state.showDates){const d=document.createElement('div');d.className='date-badge';d.textContent=fmtDate(state.photos[i].date);piece.appendChild(d);}attachPhotoGestures(piece,i);stage.appendChild(piece);});updatePieces();renderFrameLayer();renderBoundaryLayer();updateAdjustPanel();}
 function selectPhoto(index){state.selected=index;stage.querySelectorAll('.piece').forEach(el=>el.classList.toggle('selected',Number(el.dataset.index)===index));if($('#panel-order').classList.contains('active'))renderOrder();updateAdjustPanel();}
-function positionDateBadge(d,bb,sw,sh){const pad=10,top=Math.max(0,(bb.y+bb.h)*sh-40);d.style.top=`${top}px`;d.style.right='auto';d.style.transform='none';if(state.dateAlign==='left'){d.style.left=`${bb.x*sw+pad}px`;}else if(state.dateAlign==='right'){d.style.left='auto';d.style.right=`${(1-bb.x-bb.w)*sw+pad}px`;}else{d.style.left=`${(bb.x+bb.w/2)*sw}px`;d.style.transform='translateX(-50%)';}}
+function positionDateBadge(d,bb,sw,sh){const pad=10,framePx=state.frameWidth>0?Math.max(.8,state.frameWidth*(sw/700)):0,bottomGap=Math.max(8,framePx/2+7),h=Math.max(34,d.offsetHeight||34),top=Math.max(bb.y*sh+4,(bb.y+bb.h)*sh-h-bottomGap);d.style.top=`${top}px`;d.style.right='auto';d.style.transform='none';if(state.dateAlign==='left'){d.style.left=`${bb.x*sw+pad+framePx/2}px`;}else if(state.dateAlign==='right'){d.style.left='auto';d.style.right=`${(1-bb.x-bb.w)*sw+pad+framePx/2}px`;}else{d.style.left=`${(bb.x+bb.w/2)*sw}px`;d.style.transform='translateX(-50%)';}}
 function updatePieces(){if(!state.photos.length)return;const ps=polygons(),sw=stage.clientWidth,sh=stage.clientHeight;ps.forEach((poly,i)=>{const piece=stage.querySelector(`.piece[data-index="${i}"]`);if(!piece)return;const p=state.photos[i],bb=bbox(poly),img=piece.querySelector('img');piece.style.clipPath=polyCss(poly);const bw=Math.max(1,bb.w*sw),bh=Math.max(1,bb.h*sh),fit=Math.min(bw/p.w,bh/p.h),dw=p.w*fit*p.scale,dh=p.h*fit*p.scale;img.style.width=`${dw}px`;img.style.height=`${dh}px`;img.style.left=`${(bb.x+bb.w/2+p.panX*bb.w)*100}%`;img.style.top=`${(bb.y+bb.h/2+p.panY*bb.h)*100}%`;const d=piece.querySelector('.date-badge');if(d){d.textContent=fmtDate(p.date);positionDateBadge(d,bb,sw,sh);}});}
 function attachPhotoGestures(el,index){el.addEventListener('wheel',e=>{if(state.mode!=='photo')return;e.preventDefault();selectPhoto(index);state.photos[index].scale=clamp(state.photos[index].scale*(e.deltaY<0?1.07:.93),.35,10);updatePieces();},{passive:false});}
 const pointers=new Map();let gesture=null;
 function pDist(a,b){return Math.hypot(a.x-b.x,a.y-b.y)}function pCenter(a,b){return{x:(a.x+b.x)/2,y:(a.y+b.y)/2}}
 function pointerPieceIndex(e){const el=e.target.closest?.('.piece');return el?Number(el.dataset.index):-1;}
 function beginTwoFinger(){if(pointers.size!==2)return;const [a,b]=[...pointers.values()];const dist=Math.max(10,pDist(a,b)),c=pCenter(a,b);if(a.index>=0&&a.index===b.index&&state.mode==='photo'){selectPhoto(a.index);gesture={type:'photoPinch',index:a.index,startDist:dist,startScale:state.photos[a.index].scale};}else{gesture={type:'canvasPinch',startDist:dist,startZoom:state.editorZoom,startCenter:c,startScrollLeft:stageZone.scrollLeft,startScrollTop:stageZone.scrollTop};}}
-stage.addEventListener('pointerdown',e=>{if(e.target.closest?.('.handle'))return;const idx=pointerPieceIndex(e);pointers.set(e.pointerId,{x:e.clientX,y:e.clientY,index:idx,lastX:e.clientX,lastY:e.clientY});stage.setPointerCapture?.(e.pointerId);if(pointers.size===1&&state.mode==='photo'&&idx>=0){selectPhoto(idx);gesture={type:'photoDrag',index:idx,pointerId:e.pointerId,lastX:e.clientX,lastY:e.clientY};}else if(pointers.size===2)beginTwoFinger();e.preventDefault();},{passive:false});
-stage.addEventListener('pointermove',e=>{const q=pointers.get(e.pointerId);if(!q)return;q.x=e.clientX;q.y=e.clientY;pointers.set(e.pointerId,q);if(pointers.size>=2){if(!gesture||gesture.type==='photoDrag')beginTwoFinger();const [a,b]=[...pointers.values()].slice(0,2),dist=Math.max(10,pDist(a,b)),c=pCenter(a,b);if(gesture?.type==='photoPinch'){const p=state.photos[gesture.index];if(p){p.scale=clamp(gesture.startScale*(dist/gesture.startDist),.35,10);updatePieces();}}else if(gesture?.type==='canvasPinch'){const z=clamp(gesture.startZoom*(dist/gesture.startDist),1,4);if(Math.abs(z-state.editorZoom)>.004){state.editorZoom=z;fitStage();updatePieces();renderFrameLayer();}stageZone.scrollLeft=gesture.startScrollLeft-(c.x-gesture.startCenter.x);stageZone.scrollTop=gesture.startScrollTop-(c.y-gesture.startCenter.y);}e.preventDefault();return;}if(gesture?.type==='photoDrag'&&gesture.pointerId===e.pointerId&&state.mode==='photo'){const p=state.photos[gesture.index],poly=polygons()[gesture.index],bb=bbox(poly);if(p){p.panX=clamp(p.panX+(e.clientX-gesture.lastX)/Math.max(60,bb.w*stage.clientWidth),-1.8,1.8);p.panY=clamp(p.panY+(e.clientY-gesture.lastY)/Math.max(60,bb.h*stage.clientHeight),-1.8,1.8);gesture.lastX=e.clientX;gesture.lastY=e.clientY;updatePieces();}e.preventDefault();}},{passive:false});
-function endPointer(e){pointers.delete(e.pointerId);if(pointers.size<2)gesture=null;}stage.addEventListener('pointerup',endPointer);stage.addEventListener('pointercancel',endPointer);
+stageZone.addEventListener('pointerdown',e=>{if(e.target.closest?.('.handle'))return;const idx=pointerPieceIndex(e);pointers.set(e.pointerId,{x:e.clientX,y:e.clientY,index:idx,lastX:e.clientX,lastY:e.clientY});stageZone.setPointerCapture?.(e.pointerId);if(pointers.size===1&&state.mode==='photo'&&idx>=0){selectPhoto(idx);gesture={type:'photoDrag',index:idx,pointerId:e.pointerId,lastX:e.clientX,lastY:e.clientY};}else if(pointers.size===1&&idx<0&&state.editorZoom>1.01){gesture={type:'canvasPan',pointerId:e.pointerId,lastX:e.clientX,lastY:e.clientY};}else if(pointers.size===2)beginTwoFinger();e.preventDefault();},{passive:false});
+stageZone.addEventListener('pointermove',e=>{const q=pointers.get(e.pointerId);if(!q)return;q.x=e.clientX;q.y=e.clientY;pointers.set(e.pointerId,q);if(pointers.size>=2){if(!gesture||gesture.type==='photoDrag'||gesture.type==='canvasPan')beginTwoFinger();const [a,b]=[...pointers.values()].slice(0,2),dist=Math.max(10,pDist(a,b)),c=pCenter(a,b);if(gesture?.type==='photoPinch'){const p=state.photos[gesture.index];if(p){p.scale=clamp(gesture.startScale*(dist/gesture.startDist),.35,10);updatePieces();}}else if(gesture?.type==='canvasPinch'){const z=clamp(gesture.startZoom*(dist/gesture.startDist),1,4);if(Math.abs(z-state.editorZoom)>.004){const oldZoom=state.editorZoom;state.editorZoom=z;fitStage();updatePieces();renderFrameLayer();const zr=stageZone.getBoundingClientRect(),ratio=z/Math.max(.001,oldZoom),cx=c.x-zr.left,cy=c.y-zr.top;stageZone.scrollLeft=(stageZone.scrollLeft+cx)*ratio-cx;stageZone.scrollTop=(stageZone.scrollTop+cy)*ratio-cy;}stageZone.scrollLeft-=c.x-gesture.startCenter.x;stageZone.scrollTop-=c.y-gesture.startCenter.y;gesture.startCenter=c;}e.preventDefault();return;}if(gesture?.type==='photoDrag'&&gesture.pointerId===e.pointerId&&state.mode==='photo'){const p=state.photos[gesture.index],poly=polygons()[gesture.index],bb=bbox(poly);if(p){p.panX=clamp(p.panX+(e.clientX-gesture.lastX)/Math.max(60,bb.w*stage.clientWidth),-1.8,1.8);p.panY=clamp(p.panY+(e.clientY-gesture.lastY)/Math.max(60,bb.h*stage.clientHeight),-1.8,1.8);gesture.lastX=e.clientX;gesture.lastY=e.clientY;updatePieces();}e.preventDefault();return;}if(gesture?.type==='canvasPan'&&gesture.pointerId===e.pointerId){stageZone.scrollLeft-=e.clientX-gesture.lastX;stageZone.scrollTop-=e.clientY-gesture.lastY;gesture.lastX=e.clientX;gesture.lastY=e.clientY;e.preventDefault();}},{passive:false});
+function endPointer(e){pointers.delete(e.pointerId);if(!pointers.size)gesture=null;else if(pointers.size===1&&gesture?.type!=='photoDrag'&&gesture?.type!=='canvasPan')gesture=null;}stageZone.addEventListener('pointerup',endPointer);stageZone.addEventListener('pointercancel',endPointer);
 $('#fitCanvas').onclick=()=>{state.editorZoom=1;stageZone.scrollLeft=0;stageZone.scrollTop=0;fitStage();updatePieces();renderFrameLayer();};
 function showGestureTip(){const t=$('#gestureTip');t.classList.add('show');clearTimeout(window._gestureTip);window._gestureTip=setTimeout(()=>t.classList.remove('show'),3200);}
 function updateModeButtons(){const p=state.mode==='photo',b=state.mode==='boundary';$('#photoModeBtn').classList.toggle('active',p);$('#boundaryModeBtn').classList.toggle('active',b);}
@@ -130,7 +130,7 @@
 function clipPoly(ctx,poly,W,H){ctx.beginPath();poly.forEach(([x,y],i)=>{const X=x*W,Y=y*H;i?ctx.lineTo(X,Y):ctx.moveTo(X,Y)});ctx.closePath();ctx.clip();}
 async function loadImg(src){return await new Promise((res,rej)=>{const i=new Image();i.onload=()=>res(i);i.onerror=rej;i.src=src;});}
 function dateDrawX(bb,W,tw,pad){if(state.dateAlign==='left')return {x:bb.x*W+pad,align:'left',rx:bb.x*W+pad*.5};if(state.dateAlign==='right')return {x:(bb.x+bb.w)*W-pad,align:'right',rx:(bb.x+bb.w)*W-tw-pad*1.5};return {x:(bb.x+bb.w/2)*W,align:'center',rx:(bb.x+bb.w/2)*W-tw/2-pad};}
-async function buildCanvas(){const spec=ratioSpecs[state.ratio],W=spec.w,H=spec.h,note=$('#note').value.trim(),noteH=note?Math.round(H*.09):0,imageH=H-noteH,c=document.createElement('canvas');c.width=W;c.height=H;const ctx=c.getContext('2d');ctx.fillStyle='#eaf2fb';ctx.fillRect(0,0,W,H);const ps=polygons();for(let i=0;i<state.photos.length;i++){const p=state.photos[i],poly=ps[i],bb=bbox(poly),img=await loadImg(p.src);ctx.save();clipPoly(ctx,poly,W,imageH);ctx.fillStyle='#eaf2fb';ctx.fillRect(0,0,W,imageH);const bw=bb.w*W,bh=bb.h*imageH,fit=Math.min(bw/p.w,bh/p.h),dw=p.w*fit*p.scale,dh=p.h*fit*p.scale,cx=(bb.x+bb.w/2+p.panX*bb.w)*W,cy=(bb.y+bb.h/2+p.panY*bb.h)*imageH;ctx.drawImage(img,cx-dw/2,cy-dh/2,dw,dh);if(state.showDates){const txt=fmtDate(p.date);if(txt){const size=Math.max(44,Math.round(Math.min(W,H)*.044));ctx.font=`850 ${size}px system-ui,sans-serif`;const tw=ctx.measureText(txt).width,pad=size*.40,pos=dateDrawX(bb,W,tw,pad),y=(bb.y+bb.h)*imageH-size*.72;ctx.fillStyle='rgba(255,255,255,.84)';roundRect(ctx,pos.rx,y-size*.72,tw+pad*2,size*1.20,size*.30);ctx.fill();ctx.fillStyle='#17324f';ctx.textAlign=pos.align;ctx.textBaseline='middle';ctx.fillText(txt,pos.x,y-size*.12);}}ctx.restore();}if(state.frameWidth>0){const fw=Math.max(2,state.frameWidth*(W/700));ctx.save();ctx.strokeStyle='#fff';ctx.lineJoin='miter';ctx.lineCap='butt';ctx.lineWidth=fw;for(const seg of boundarySegments()){ctx.beginPath();ctx.moveTo(seg.p1[0]*W,seg.p1[1]*imageH);ctx.lineTo(seg.p2[0]*W,seg.p2[1]*imageH);ctx.stroke();}ctx.strokeRect(fw/2,fw/2,Math.max(0,W-fw),Math.max(0,imageH-fw));ctx.restore();}if(noteH){ctx.fillStyle='#fff';ctx.fillRect(0,imageH,W,noteH);ctx.strokeStyle='#d4e0ed';ctx.lineWidth=2;ctx.beginPath();ctx.moveTo(0,imageH);ctx.lineTo(W,imageH);ctx.stroke();ctx.fillStyle='#17324f';ctx.textAlign='center';ctx.textBaseline='middle';ctx.font=`650 ${Math.round(noteH*.32)}px system-ui,sans-serif`;drawWrap(ctx,note,W/2,imageH+noteH/2,W*.9,noteH*.38,2);}return c;}
+async function buildCanvas(){const spec=ratioSpecs[state.ratio],W=spec.w,H=spec.h,note=$('#note').value.trim(),noteH=note?Math.round(H*.09):0,imageH=H-noteH,fw=state.frameWidth>0?Math.max(2,state.frameWidth*(W/700)):0,c=document.createElement('canvas');c.width=W;c.height=H;const ctx=c.getContext('2d');ctx.fillStyle='#eaf2fb';ctx.fillRect(0,0,W,H);const ps=polygons();for(let i=0;i<state.photos.length;i++){const p=state.photos[i],poly=ps[i],bb=bbox(poly),img=await loadImg(p.src);ctx.save();clipPoly(ctx,poly,W,imageH);ctx.fillStyle='#eaf2fb';ctx.fillRect(0,0,W,imageH);const bw=bb.w*W,bh=bb.h*imageH,fit=Math.min(bw/p.w,bh/p.h),dw=p.w*fit*p.scale,dh=p.h*fit*p.scale,cx=(bb.x+bb.w/2+p.panX*bb.w)*W,cy=(bb.y+bb.h/2+p.panY*bb.h)*imageH;ctx.drawImage(img,cx-dw/2,cy-dh/2,dw,dh);if(state.showDates){const txt=fmtDate(p.date);if(txt){const size=Math.max(44,Math.round(Math.min(W,H)*.044));ctx.font=`850 ${size}px system-ui,sans-serif`;const tw=ctx.measureText(txt).width,pad=size*.40,pos=dateDrawX(bb,W,tw,pad),badgeH=size*1.20,bottomGap=Math.max(size*.16,fw/2+size*.10),badgeBottom=(bb.y+bb.h)*imageH-bottomGap,badgeTop=badgeBottom-badgeH,textY=badgeTop+badgeH/2;ctx.fillStyle='rgba(255,255,255,.84)';roundRect(ctx,pos.rx,badgeTop,tw+pad*2,badgeH,size*.30);ctx.fill();ctx.fillStyle='#17324f';ctx.textAlign=pos.align;ctx.textBaseline='middle';ctx.fillText(txt,pos.x,textY);}}ctx.restore();}if(state.frameWidth>0){ctx.save();ctx.strokeStyle='#fff';ctx.lineJoin='miter';ctx.lineCap='butt';ctx.lineWidth=fw;for(const seg of boundarySegments()){ctx.beginPath();ctx.moveTo(seg.p1[0]*W,seg.p1[1]*imageH);ctx.lineTo(seg.p2[0]*W,seg.p2[1]*imageH);ctx.stroke();}ctx.strokeRect(fw/2,fw/2,Math.max(0,W-fw),Math.max(0,imageH-fw));ctx.restore();}if(noteH){ctx.fillStyle='#fff';ctx.fillRect(0,imageH,W,noteH);ctx.strokeStyle='#d4e0ed';ctx.lineWidth=2;ctx.beginPath();ctx.moveTo(0,imageH);ctx.lineTo(W,imageH);ctx.stroke();ctx.fillStyle='#17324f';ctx.textAlign='center';ctx.textBaseline='middle';ctx.font=`650 ${Math.round(noteH*.32)}px system-ui,sans-serif`;drawWrap(ctx,note,W/2,imageH+noteH/2,W*.9,noteH*.38,2);}return c;}
 function roundRect(ctx,x,y,w,h,r){const rr=Math.min(r,w/2,h/2);ctx.beginPath();ctx.moveTo(x+rr,y);ctx.arcTo(x+w,y,x+w,y+h,rr);ctx.arcTo(x+w,y+h,x,y+h,rr);ctx.arcTo(x,y+h,x,y,rr);ctx.arcTo(x,y,x+w,y,rr);ctx.closePath();}
 function drawWrap(ctx,text,x,y,maxW,lineH,maxLines){let lines=[''];for(const ch of [...text]){let t=lines[lines.length-1]+ch;if(ctx.measureText(t).width>maxW&&lines[lines.length-1]){if(lines.length>=maxLines)break;lines.push(ch);}else lines[lines.length-1]=t;}const total=(lines.length-1)*lineH;lines.forEach((ln,i)=>ctx.fillText(ln,x,y-total/2+i*lineH));}
 async function renderExportPreview(){const c=await buildCanvas(),p=$('#exportPreview');p.width=Math.round(c.width*.25);p.height=Math.round(c.height*.25);p.getContext('2d').drawImage(c,0,0,p.width,p.height);const spec=ratioSpecs[state.ratio];$('#exportHelp').textContent=`PNG 為無損輸出；JPG 使用高畫質。${spec.label} 輸出 ${spec.w}×${spec.h}。`;}
